# Deprecated — replaced by `.github/workflows/pr-automerge.yml` (all PRs, green CI).
name: Cursor AI auto-merge (deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto-merge moved to pr-automerge.yml"
